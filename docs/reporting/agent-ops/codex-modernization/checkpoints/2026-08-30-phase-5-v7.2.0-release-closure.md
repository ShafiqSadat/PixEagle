# Phase 5 v7.2.0 Release Closure

Date: 2026-08-30
Issue: PXE-0170
Status: software release gates complete

## Scope

- remove redundant server-side STUN delay on directly routed public IPv4 hosts
  without changing browser STUN, private/NAT behavior, or TURN;
- add bounded redacted diagnostics for failed browser WebRTC attempts and peer
  cleanup;
- retain the final command-preview intent summary after shutdown while keeping
  the preview path incapable of PX4/MAVSDK publication;
- synchronize generated API/MCP provenance and repair two dashboard timer tests
  that encoded an obsolete polling interval;
- align retirement-registry validator tests with the intentional process-pinned
  config-definition generation while retaining explicit uncached candidate
  validation coverage.

## Changed Areas

- `src/classes/webrtc_manager.py`
- `dashboard/src/components/VideoStream.js`
- `src/classes/command_preview.py`
- focused backend and dashboard tests
- WebRTC, command-preview, changelog, and modernization records
- generated API/MCP candidate provenance

## Validation

- focused WebRTC and command-preview backend: `44 passed`;
- mandatory Phase 0, schema, API/security/docs/setup contracts: `509 passed`;
- dashboard: `59` suites / `405` tests passed;
- dashboard ESLint and optimized production build passed;
- config schema/retirement integration: `9 passed`;
- touched Python modules compiled and `git diff --check` passed;
- independent remote Google Chrome 139 logged in to the authenticated public lab
  and rendered advancing 640x480 WebRTC video for 22 seconds in Auto mode;
- server answer generation took about 6 ms instead of the prior repeatable
  five-second STUN gather, ICE reached `completed`, and bounded cleanup recorded
  4,707,061 video bytes and 3,968 packets sent;
- refreshed runtime `pixeagle_manual_fb5991d0-20d7-4a62-b36f-83398e3ec34a`
  remained healthy and returned dashboard HTTP 200.

Runtime evidence is in
`logs/runtime/pixeagle_manual_fb5991d0-20d7-4a62-b36f-83398e3ec34a/` on the
acceptance host. It is operational evidence, not a tracked release artifact.

## Claim Boundary

This proves the tested direct-public host and remote browser path. It does not
prove restrictive NAT traversal, TURN deployment, production TLS/WSS, target
hardware or camera cadence, PX4/SIH/SITL/HIL behavior, aircraft response,
flight, field suitability, or regulatory acceptance. CSRT remains the default;
the newer classic trackers remain opt-in candidates with their documented
evidence limits.

## Remaining Work

- obtain Raspberry Pi/Jetson/Ubuntu actual-camera cadence and annotated tracker
  identity/recovery evidence;
- validate TURN only for deployments that require relay traversal;
- run the separately approved PX4/SIH/SITL/HIL and physical operator gates
  before making vehicle or field claims.
